## 1️⃣ What is the difference between var, let, and const?
Ans: var is old variable declare system
its not blick scoped, its functional scoped.
we can reassign 
we can redeclare 
its hosting. 
    
let is modern js es6 declaration
its block scoped
we can reassign value
but can't redeclare

const is constent variable declaration
its block scoped
can't reassign
can't redeclare

## 2️⃣ What is the spread operator (...)?
Ans: spread operator we use for array/object copy , array/object merge. its do all element spread.

## 3️⃣ What is the difference between map(), filter(), and forEach()?
Ans: map(), filter() and ferEach for use looping but they have deffrent working system.  

forEach working loop in array element. it's no retrun new array. only looping.

map() it will use for modify every single element of array.
return new array. length same.

filter() it use for find element with condition. return new array and remove unnecessary element.
 

## 4️⃣ What is an arrow function?
Ans: Arrow function is short function system in js es6,
single line it will auto return. if multipole condition then have to write return.

## 5️⃣ What are template literals?
Ans: Template literals is js modern feture es6. it will use easy string and variable and  set dynamic value.
